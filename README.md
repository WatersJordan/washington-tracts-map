# Washington Tracts

Interactive map generated from a QGIS project.

The map data is encrypted at rest with AES-256-GCM. Opening the page prompts
for a passphrase, which is required before anything renders. Without it this
repository contains no readable property information.

Single self-contained page, no build step and no dependencies to install.
